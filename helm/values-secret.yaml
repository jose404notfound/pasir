# This file contains sensitive information and should not be committed to version control.
# It is recommended to use a secret management tool or environment variables for production deployments.
# Add this file to your .gitignore.
mysql:
  rootPassword: "rootpassword"
  database: "auth_db"
  user: "user"
  password: "password"
